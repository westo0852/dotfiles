[init]
	defaultBranch = main
[core]
	editor = nvim
	excludesfile = /home/iw/.gitignore
	pager = diff-so-fancy | less --tabs=4 -RF
[interactive]
	diffFilter = diff-so-fancy --patch
[pull]
	rebase = true
